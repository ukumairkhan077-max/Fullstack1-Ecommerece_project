# Copy this file to ".env" (for local dev) to point the frontend at your
# backend API. If this isn't set, the app falls back to
# http://localhost:5000/api automatically.
VITE_API_URL=http://localhost:5000/api
